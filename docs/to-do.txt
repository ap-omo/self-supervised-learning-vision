- instead of one big VAE for a whole image, learn how to implement many small VAEs that learn locally across small patches of the image then stack them hierarchically on top of each other

- in order to enable local self-supervised learning, need to figure out a suitable approach (through a function or other means) to find optimal parameter values for the input image (bearing in mind input images are random), keeping in mind efficiency and computational power in relation to quality of output
    - building on to this, maybe a machine learning within a machine learning approach where parameters are changed after comparing input images to reconstructions and generations

- have a quick look at https://keras.io/api/models/model_training_apis/ (different model training methods) to see if it can be useful
