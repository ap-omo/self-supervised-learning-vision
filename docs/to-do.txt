- move onto testing on different harder datasets (CelebA, CIFAR-10 etc.)
- expand CNN by adding more layers during encoding/decoding for better results
- instead of one big VAE for a whole image, implement many small VAEs that learn locally across small patches of the image then stack them hierarchically on top of each other
- plan mid project demonstration
- evaluate outputs from different parameters and figure out what suits different types of images
- as input images are random, need to figure out a way to create a function (or a suitable approach) to find optimal parameters are selected for filters and training, keeping in mind efficiency and computational power
- have a deeper look at https://keras.io/api/models/model_training_apis/ (different model training methods) 


