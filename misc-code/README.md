Miscellaneous builds of networks, encoders etc. to further understanding and knowledge as a combination of all of them will be used or useful for the final release.

Started off with creating a simple CNN
Trained the CNN on a simple database (MNIST) instead of randomly generated pixels
Experimented and learned VAE implementation
Trained model on more complex databases, starting with CIFAR-10
